

%ipsi8902122_cf1498Hz=ipsi_data(1:22);
G=ipsi89021_22_conduction;
for n=1:22
    for k=1:length(G)
        if G(k).termx == ipsi_data(n).x & G(k).termy == ipsi_data(n).y & G(k).termz == ipsi_data(n).z
            [G(k).FBtoSegEnd-ipsi_data(n).al_from_fb G(k).Order-ipsi_data(n).branch_order]
            ipsi_data(n).previousset=G(k).previousset;
            ipsi_data(n).seglengthall=G(k).seglengthall;
            ipsi_data(n).avgdiaall=G(k).avgdiaall;
            ipsi_data(n).ctall=G(k).ctall;
            ipsi_data(n).ctsum=G(k).ctsum;
        end;
    end;
end;

%ipsi8900739_cf2470Hz=ipsi_data(23:33);
G=ipsi89007_39_conduction;
for n=23:33
    for k=1:length(G)
        if G(k).termx == ipsi_data(n).x & G(k).termy == ipsi_data(n).y & G(k).termz == ipsi_data(n).z
            [G(k).FBtoSegEnd-ipsi_data(n).al_from_fb G(k).Order-ipsi_data(n).branch_order]
            ipsi_data(n).previousset=G(k).previousset;
            ipsi_data(n).seglengthall=G(k).seglengthall;
            ipsi_data(n).avgdiaall=G(k).avgdiaall;
            ipsi_data(n).ctall=G(k).ctall;
            ipsi_data(n).ctsum=G(k).ctsum;
        end;
    end;
end;

%ipsi8900743_cf2694Hz=ipsi_data(34:40);
G=ipsi89007_43_conduction;
for n=34:40
    for k=1:length(G)
        if G(k).termx == ipsi_data(n).x & G(k).termy == ipsi_data(n).y & G(k).termz == ipsi_data(n).z
            [G(k).FBtoSegEnd-ipsi_data(n).al_from_fb G(k).Order-ipsi_data(n).branch_order]
            ipsi_data(n).previousset=G(k).previousset;
            ipsi_data(n).seglengthall=G(k).seglengthall;
            ipsi_data(n).avgdiaall=G(k).avgdiaall;
            ipsi_data(n).ctall=G(k).ctall;
            ipsi_data(n).ctsum=G(k).ctsum;
        end;
    end;
end;

%ipsi9004315_cf2300Hz=ipsi_data(41:70);
G=ipsi90043_15_conduction;
for n=41:70
    for k=1:length(G)
        if G(k).termx == ipsi_data(n).x & G(k).termy == ipsi_data(n).y & G(k).termz == ipsi_data(n).z
            [G(k).FBtoSegEnd-ipsi_data(n).al_from_fb G(k).Order-ipsi_data(n).branch_order]
            ipsi_data(n).previousset=G(k).previousset;
            ipsi_data(n).seglengthall=G(k).seglengthall;
            ipsi_data(n).avgdiaall=G(k).avgdiaall;
            ipsi_data(n).ctall=G(k).ctall;
            ipsi_data(n).ctsum=G(k).ctsum;
        end;
    end;
end;

%ipsi8705624_cf1903Hz=ipsi_data(71:81);
G=ipsi87056_24_conduction;
for n=71:81
    for k=1:length(G)
        if G(k).termx == ipsi_data(n).x & G(k).termy == ipsi_data(n).y & G(k).termz == ipsi_data(n).z
            [G(k).FBtoSegEnd-ipsi_data(n).al_from_fb G(k).Order-ipsi_data(n).branch_order]
            ipsi_data(n).previousset=G(k).previousset;
            ipsi_data(n).seglengthall=G(k).seglengthall;
            ipsi_data(n).avgdiaall=G(k).avgdiaall;
            ipsi_data(n).ctall=G(k).ctall;
            ipsi_data(n).ctsum=G(k).ctsum;
        end;
    end;
end;

%ipsi8831120_cf200Hz=ipsi_data(82:117);
G=ipsi88311_20_conduction;
for n=82:117
    for k=1:length(G)
        if G(k).termx == ipsi_data(n).x & G(k).termy == ipsi_data(n).y & G(k).termz == ipsi_data(n).z
            [G(k).FBtoSegEnd-ipsi_data(n).al_from_fb G(k).Order-ipsi_data(n).branch_order]
            ipsi_data(n).previousset=G(k).previousset;
            ipsi_data(n).seglengthall=G(k).seglengthall;
            ipsi_data(n).avgdiaall=G(k).avgdiaall;
            ipsi_data(n).ctall=G(k).ctall;
            ipsi_data(n).ctsum=G(k).ctsum;
        end;
    end;
end;

%ipsi880113_cf5388Hz=ipsi_data(118:121);
G=ipsi88011_3_conduction;
for n=118:121
    for k=1:length(G)
        if G(k).termx == ipsi_data(n).x & G(k).termy == ipsi_data(n).y & G(k).termz == ipsi_data(n).z
            [G(k).FBtoSegEnd-ipsi_data(n).al_from_fb G(k).Order-ipsi_data(n).branch_order]
            ipsi_data(n).previousset=G(k).previousset;
            ipsi_data(n).seglengthall=G(k).seglengthall;
            ipsi_data(n).avgdiaall=G(k).avgdiaall;
            ipsi_data(n).ctall=G(k).ctall;
            ipsi_data(n).ctsum=G(k).ctsum;
        end;
    end;
end;







