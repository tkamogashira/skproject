function ds = EmptyDataset(ds);
% EmptyDataset - remove data from dataset to save memory (only parameters survive)
%
% See also FillDataset

ds.Data = 'Data have been removed by emptyDataSet';

